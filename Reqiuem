list_num = [1,4,5,6,7,9]
a = list_num[0]    # Переменная а ровна первому числу из списка
for b in list_num: # Переменная b принимает значения из списка по порядку
    if b > a:      # Если переменная b больше a, которая принимает первое значение из списка
        a = b      #... тогда ее значение переходит в a
print(a)
